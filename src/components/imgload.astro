---
import { Picture } from "astro:assets";

interface Props {
    src: ImageMetadata;
    alt: string;
    width: number;
}

const { src, alt, width } = Astro.props;
---

<Picture
    src={src}
    alt={alt}
    formats={["avif", "webp"]}
    width={width}
    densities={[1, 2, 3]}
/>
